#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    int wnum = 5;
    double dnum = 5.5;
    cout << wnum;
    wnum++;
    /* Math done with two integers will return two integers */
    cout << pow(2, 5); /*sqrt(36) round(4.3) ceil(4.1) = 5 floor(5.7) = 4 fmax(3, 10) = 10, fmin()*/
    return 0;
}
